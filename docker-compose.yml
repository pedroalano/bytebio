services:
  web:
    build:
      context: .
      target: runner
    image: bytebio:latest
    container_name: bytebio-web
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
